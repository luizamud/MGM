<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="luizfelipemoralez.amuds.mygymmanager">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.Cadastro_Treino" android:parentActivityName=".Controller.Home"/>
        <activity android:name=".Controller.App_Autoria" />
        <activity android:name=".Model.Treino" />
        <activity android:name=".Controller.Ficha_Usuario"  android:parentActivityName=".Controller.Home"></activity>
        <activity android:name=".Controller.Home" />
        <activity android:name=".Controller.StartApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>